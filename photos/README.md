Drop team portraits here and run `python3 src/build.py` to embed them in the
site. The filename (lowercased, without extension) must match a card's photo
key in src/part_core.js TEAM_PAGE — currently: cavanaugh, tierney.
Portraits are downscaled to ~480px on embed, so any phone photo works.
